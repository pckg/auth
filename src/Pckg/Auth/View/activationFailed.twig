{% extends extends %}

{% block content %}
    <div class="row">
        <div class="col-md-4">
            <div class="page-header">
                <h1>{{ __('h1_activation_failed') }}</h1>
            </div>
            {{ __('text_activation_failed') }}
        </div>
    </div>
{% endblock %}